
{% extends 'layout.html.twig' %}



{% block content %}


    {{ form_start(formulario) }}
    {{ form_widget(formulario) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <a href="{{ path('mostrar_juergas') }}" class="btn btn-info"><i class="fa fa-backward"></i> Volver</a>
            {% if evento.id %}
            {% if is_granted('EVENTO_MODIFICAR', evento) %}<button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Guardar cambios</button>
           <a href="{{ path('evento_eliminar', {'id': evento.id}) }}" class="btn btn-danger"><i class="fa fa-trash"></i> Eliminar</a>{% endif %}{% endif %}

        </div>
    </div>
    {{ form_end(formulario) }}
{% endblock %}
