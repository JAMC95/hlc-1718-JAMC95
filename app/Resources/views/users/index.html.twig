{% extends 'layout.html.twig' %}

{% block content %}


    <br/>
    <table class="table table-bordered table-hover table-striped">
        <thead>
        <tr>
            <th>Nombre</th>
            <th>¿Admin?</th>

        </tr>
        </thead>
        <tbody>
        {% for usuario in usuarios %}
            <tr>
                <td>{{ usuario.nombreUsuario }}</td>
                <td>{{ usuario.admin ? 'Si':'No' }}</td>


            </tr>
        {% else %}
            <tr>
                <td colspan="5">No hay eventos que mostrar</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('mostrar_juergas') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Volver a la portada</a>


{% endblock %}
